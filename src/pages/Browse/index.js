import React from 'react';
import ContentCarousel from "@/components/Carousel";

const Browse = () => {
    return (
        <div>
            <ContentCarousel/>
        </div>
    );
};

export default Browse;
