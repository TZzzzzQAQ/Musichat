import AuthRoute from "@/components/AuthRoute.jsx";

const Recent = () => {
    return (
        <AuthRoute>
            <div>
                This is Recent!
            </div>
        </AuthRoute>
    );
};

export default Recent;
