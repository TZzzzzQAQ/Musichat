import ContentCarousel from "@/components/Carousel.jsx";

const Browse = () => {
    return (
        <div className={'h-full'}>
            <ContentCarousel/>
        </div>
    );
};

export default Browse;
